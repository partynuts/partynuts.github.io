weaponBox.addEventListener('click', gamePlay.weaponChoiceHuman);

startFightBtn.addEventListener('click', gamePlay.startingBattleComputerComputer);

tryAgainBtn.addEventListener('click', gameUI.tryAgain);

startNewGameBtn.addEventListener('click', gameUI.startNewGame);

rulesBtn.addEventListener('click', gameUI.showRules);

changePlayerModeBtn.addEventListener('click', gameUI.switchMode);

closeRulesBtn.addEventListener('click', gameUI.closeRulesModal);

closeInfoBtn.addEventListener('click', gameUI.closeInfoModal);

window.addEventListener('click', gameUI.closeModal);